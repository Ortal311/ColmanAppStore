@model ColmanAppStore.Models.App

@{
    ViewData["Title"] = "Details";
}


<center>
    <div class="card col-12" style="width: 50rem;
        margin-top: 10px;
        margin-right: 10px;
        margin-left: 10px;
        margin-bottom: 10px;">

        <center><img src="@Model.Logo.Image" class="card-img-top" alt="can't load image" style="max-width: 15%;"></center>
        <div class="card-body">
            <h5 class="card-title">@Model.Name</h5>
            <p class="card-text" style="text-align:left">Price: @Model.Price ₪</p>
            <p class="card-text" style="text-align:left">App size: @Model.Size MB</p>

            <p class="card-text" style="text-align:left">Description: @Model.Description</p>
            <p class="card-text" style="text-align:left">Publish date: @Model.publishDate.Date</p>
            <p class="card-text" style="text-align:left">Developer name: @Model.DeveloperName</p>


            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                <ol class="carousel-indicators">
                    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
                    <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
                    <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
                </ol>
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img class="d-block w-100" src="https://mdbootstrap.com/img/Photos/Slides/img%20(15).jpg" alt="First slide">
                    </div>
                    <div class="carousel-item">
                        <img class="d-block w-100" src="https://mdbootstrap.com/img/Photos/Slides/img%20(22).jpg" alt="Second slide">
                    </div>
                    <div class="carousel-item">
                        <img class="d-block w-100" src="https://mdbootstrap.com/img/Photos/Slides/img%20(23).jpg" alt="Third slide">
                    </div>
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>






            <!-- Carousel wrapper -->
            @*<p class="card-text" style="text-align:left">
        @if (Model.Images != null)
        {
            @foreach (var item in Model.Images)
            {
            <p>@item.Image</p>
        }
        }
        else
        {
            <p>no images added for the app</p>//NOT SHOWING IN WEB!!! TO FIX
        }
        </p>*@

            @*pics
        videos
        average rate
        comments*@



            @*<a href="/Apps/Details/@Model.Id" class="btn btn-primary">More info</a>*@
            <div>
                <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
                <a asp-action="Index">Back to List</a>
            </div>
        </div>
        </div>
    </center>


    <!--<!--<dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.Images)
    </dt>
    <dd class="col-sm-10">-->
    -->
    <!--@*@Html.DisplayFor(model => model.Images.Image)*@-->
    <!--<!--@if (Model.Images != null)
        {
            @foreach (var item in Model.Images)
            {
                <p>@item.Image</p>
            }
        }
        else
        {
            <p>no images added for the app</p>//NOT SHOWING IN WEB!!! TO FIX
        }
    </dd>

    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.Videos)
    </dt>
    <dd class="col-sm-10">
        @if (Model.Videos != null)
        {
            @foreach (var item in Model.Videos)
            {
                <p>@item.Video</p>
            }
        }
        else
        {
            <p>no videos added for the app</p>
        }-->
    -->
    <!--@*@Html.DisplayFor(model => model.Videos.Video)*@-->
    <!--</dd>-->

    <div>
        <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
        <a asp-action="Index">Back to List</a>
    </div>
